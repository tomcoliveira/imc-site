import Image from 'next/image'; import Link from 'next/link'; import {CLINICS} from '../clinics.data';
export function generateStaticParams(){return CLINICS.map(c=>({slug:c.slug}))}
export default function ClinicGallery({params}:{params:{slug:string}}){const clinic=CLINICS.find(c=>c.slug===params.slug); if(!clinic)return <div className="container py-10">Clínica não encontrada.</div>; const range=Array.from({length:clinic.photos},(_,i)=>String(i+1).padStart(2,'0')); return(<main className="container py-12"><div className="mb-6 flex items-center justify-between gap-4"><div><h1 className="text-2xl font-bold">{clinic.name}</h1><div className="text-slate-600">{clinic.city}</div></div><a href="https://wa.me/5511971709603" target="_blank" rel="noreferrer" className="rounded-xl bg-emerald-500 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-600">Falar no WhatsApp</a></div><div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">{range.map(id=>(<Image key={id} src={`/clinicas/${clinic.slug}/${id}.jpg`} alt={`${clinic.name} foto ${id}`} width={1200} height={800} className="w-full h-auto rounded-xl border border-slate-200 object-cover" />))}</div><div className="mt-8 flex items-center justify-between"><Link href="/clinicas" className="text-sm text-slate-700 hover:underline">← Voltar para clínicas</Link><a href="https://wa.me/5511971709603" target="_blank" rel="noreferrer" className="rounded-lg bg-emerald-500 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-600">Tenho interesse</a></div></main>)}